package com.pragyakallanagoudar.varanus.adapter;

public class FinalTaskAdapter // extends FirestoreAdapter<TasksAdapter.ViewHolder>
{
    /**
    public final String LOG_TAG = FinalTaskAdapter.class.getSimpleName(); // for debugging

    // Interface here!!
    public interface OnFinalSelectedListener
    {
        void onFinalSelected(DocumentSnapshot tasks); // implement in subclasses
    }

    private OnFinalSelectedListener mListener;  // instance of above interface

    // constructor
    public FinalTaskAdapter(Query query, OnFinalSelectedListener listener)
    {
        super(query);
        mListener = listener;
    }

    /**
    @NonNull
    @Override
    public FinalTaskAdapter.ViewHolder onCreateViewHolder(@NonNull ViewGroup parent, int viewType)
    {
        LayoutInflater inflater = LayoutInflater.from(parent.getContext());
        return new FinalTaskAdapter.ViewHolder(inflater.inflate(R.layout.item_task, parent, false));
    }
    */



}
